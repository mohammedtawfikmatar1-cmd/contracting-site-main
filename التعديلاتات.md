# التعديلاتات — توثيق شامل لتغييرات المحادثة

هذا الملف موجه للمطورين بهدف **تسهيل المراجعة (Review)** وفهم ما تم تعديله خلال هذه المحادثة:  
ما الذي تغيّر؟ لماذا؟ وأين؟ وكيف نختبر بسرعة للتأكد أن كل شيء يعمل بدون تغيير في واجهة لوحة التحكم أو واجهة الموقع.

---

## 1) تحسين محرر النصوص (Summernote) — رفع الصور كـ URL بدل Base64

### الهدف
- منع تخزين الصور داخل النص على هيئة Base64 (يؤدي إلى بطء شديد وتضخم قاعدة البيانات).
- جعل تجربة المستخدم داخل الـ Editor “طبيعية”:
  - يدرج الصورة فتظهر مباشرة.
  - عملية الرفع/استبدال الرابط تتم بالخلفية **بدون أن يشعر المستخدم**.
  - منع حفظ النموذج قبل انتهاء رفع الصور لتجنب حفظ روابط `blob:` داخل المحتوى.

### ما الذي تم تطبيقه؟
#### (أ) Endpoint لرفع صور المحرر (AJAX)
- **Route جديد (Admin)**:
  - `POST /admin/editor/upload`
  - الاسم: `admin.editor.upload`
- **Controller جديد**:
  - `app/Http/Controllers/Admin/EditorUploadController.php`
  - يقوم برفع الصورة إلى:
    - `storage/app/public/editor/{context}/...`
  - ثم يعيد JSON يحتوي:
    - `url`: رابط عام للصورة
    - `path`: مسار التخزين

> ملاحظة مهمة: تم إرجاع الرابط عبر `route('media.file', ...)` لضمان عمل الروابط حتى عند تشغيل المشروع داخل **مجلد فرعي** مثل:
> `http://localhost/project/contracting-site-main/...`

#### (ب) FormRequest لرفع صور المحرر
- `app/Http/Requests/Admin/EditorImageUploadRequest.php`
- تحقق من:
  - `image`: صورة وبحد حجم مناسب (حالياً 4MB)
  - `context`: قيمة نصية قصيرة لتحديد المجلد الفرعي (news/pages/…)

#### (ج) Partial موحد لتهيئة Summernote + رفع الصور
- ملف جديد:
  - `resources/views/admin/partials/summernote.blade.php`
- الوظائف الأساسية:
  - تهيئة المحرر لكل `.js-editor`
  - رفع الصور عبر `callbacks.onImageUpload`
  - **إدراج معاينة فورية** باستخدام `URL.createObjectURL(file)`
  - عند اكتمال الرفع: **استبدال `src` بالصورة النهائية** (URL)
  - عند الفشل: إزالة المعاينة (بدون `alert` مزعج)
  - **منع submit للنموذج** أثناء وجود عمليات رفع قيد التنفيذ ثم **حفظ تلقائي** بعد اكتمال الرفع

#### (د) إضافة CSRF token للطلبات AJAX داخل لوحة التحكم
- تعديل Layout:
  - `resources/views/admin/layouts/app.blade.php`
  - تمت إضافة:
    - `<meta name="csrf-token" content="{{ csrf_token() }}">`

#### (هـ) تطبيق الـ Partial على جميع صفحات Admin التي فيها Editor
تم تعديل صفحات كثيرة داخل:
- `resources/views/admin/...`
لتقوم بـ:
- إضافة `data-editor-context` على كل textarea محرر
- استبدال سكربتات تهيئة Summernote المكررة بـ:
  - `@include('admin.partials.summernote')`

أمثلة ملفات تم تعديلها (غير حصري):
- `resources/views/admin/news/create.blade.php`
- `resources/views/admin/news/edit.blade.php`
- `resources/views/admin/pages/create.blade.php`
- `resources/views/admin/pages/edit.blade.php`
- `resources/views/admin/projects/create.blade.php`
- `resources/views/admin/projects/edit.blade.php`
- `resources/views/admin/services/create.blade.php`
- `resources/views/admin/services/edit.blade.php`
- `resources/views/admin/jobs/create.blade.php`
- `resources/views/admin/jobs/edit.blade.php`
- `resources/views/admin/tenders/create.blade.php`
- `resources/views/admin/tenders/edit.blade.php`
- `resources/views/admin/clients/create.blade.php`
- `resources/views/admin/clients/edit.blade.php`
- `resources/views/admin/settings/about.blade.php`

---

## 2) توحيد الـ Validation عبر FormRequest (بدلاً من $request->validate داخل Controllers)

### الهدف
- فصل منطق التحقق عن الـ controllers لسهولة الاختبار والصيانة.
- السماح بمحتوى نصي طويل (عدم تقييد حقول المحتوى الطويلة بحدود صغيرة).

### ما الذي تم؟
تم إنشاء Requests باستخدام artisan ثم تم تفعيلها داخل controllers:

#### News
- Requests:
  - `app/Http/Requests/Admin/StoreNewsRequest.php`
  - `app/Http/Requests/Admin/UpdateNewsRequest.php`
- Controller:
  - `app/Http/Controllers/Admin/NewsController.php`
  - تم حذف دالة `validateNews` واستبدالها بـ `$request->validated()`

#### Pages
- Requests:
  - `StorePageRequest`, `UpdatePageRequest`
- Controller:
  - `Admin/PageController.php`
  - تم حذف دالة `validatePage`

#### Projects
- Requests:
  - `StoreProjectRequest`, `UpdateProjectRequest`
- Controller:
  - `Admin/ProjectController.php`
  - تم حذف دالة `validateProject`

#### Services
- Requests:
  - `StoreServiceRequest`, `UpdateServiceRequest`
- Controller:
  - `Admin/ServiceController.php`
  - تم حذف دالة `validateService`

#### Tenders
- Requests:
  - `StoreTenderRequest`, `UpdateTenderRequest`
- Controller:
  - `Admin/TenderController.php`
  - تم حذف دالة `validateTender`

#### Jobs
- Requests:
  - `StoreJobRequest`, `UpdateJobRequest`
- Controller:
  - `Admin/JobController.php`
  - تم حذف دالة `validateJob`

#### Clients
- Requests:
  - `StoreClientRequest`, `UpdateClientRequest`
- Controller:
  - `Admin/ClientController.php`
  - تم نقل تحقق التعارض (مشروع مرتبط بعميل آخر) للـ controller بعد `validated()`

#### Settings
- Requests:
  - `StoreSettingRequest`, `UpdateSettingRequest`
  - `SaveBrandingRequest`, `SaveAboutPageRequest`
- Controller:
  - `Admin/SettingController.php`
  - تم استبدال `$request->validate` بـ `$request->validated()`
  - ملاحظة: تم الإبقاء على `validate` إضافي صغير داخل `saveBranding` للتحقق من `theme_preset/structure_preset` لأن قيمها ديناميكية.

> ملاحظة: أغلب حقول المحتوى الطويلة أصبحت `string` بدون `max` حتى لا نحدّ النصوص مستقبلاً.

---

## 3) تحسين أداء الموقع (Caching) بدون تغيير الواجهة

### الهدف
- تقليل الاستعلامات المتكررة للزوار.
- تسريع صفحات: الرئيسية، الأرشيف، التفاصيل، الإعدادات العامة.

### ما الذي تم؟
#### (أ) كاش للإعدادات Settings
- `app/Models/Setting.php`
  - `Setting::getValue()` أصبح يستخدم `Cache::remember` لمدة 10 دقائق.
  - عند حفظ/حذف Setting يتم تفريغ الكاش تلقائياً عبر hooks في `booted()` + داخل `setValue`.
  - مفاتيح كاش مستخدمة:
    - `settings:value:{key}`
    - `site:settings:all`

#### (ب) كاش لاستعلامات الواجهة
- `app/Http/Controllers/SiteController.php`
  - كاش قصير لبيانات صفحات كثيرة (بالدقائق) مثل:
    - خدمات/مشاريع/أخبار الصفحة الرئيسية
    - صفحات الأرشيف مع page number
    - صفحات التفاصيل (service/project/news)
    - إعدادات الموقع المجمعة والإحصاءات

> ملاحظة: استخدمنا مدد قصيرة لتقليل “بيانات قديمة” وفي نفس الوقت تحسين الأداء.

---

## 4) التخزين العام وعرض الصور

### المشكلة التي كانت تظهر
روابط الصور لا تعمل أحياناً (خاصة على localhost داخل مجلد فرعي) أو عدم وجود `public/storage`.

### ما الذي تم
- تم إنشاء/تأكيد `storage:link` (على ويندوز يظهر كـ Junction).
- تم الاعتماد على مسار وسائط موحد موجود أصلاً:
  - `GET /media/{path}` باسم `media.file`
  - هذا المسار يعرض الملفات من `Storage::disk('public')` مباشرة.
- لذلك تم تعديل رفع صور المحرر ليعيد `url` عبر `media.file`.

---

## 5) تحديثات للاختبارات

### ماذا حدث؟
`php artisan test` كان يفشل بسبب عدم تشغيل migrations في بيئة الاختبار (SQLite in-memory).

### الإصلاح
- تعديل:
  - `tests/Feature/ExampleTest.php`
- تم تفعيل:
  - `use RefreshDatabase;`
لتشغيل migrations قبل الاختبار.

نتيجة الاختبار:
- `php artisan test` => **Passed**

---

## 6) ملفات Release / توثيق GitHub Releases

### تم إنشاء قالب Release
- `RELEASE_TEMPLATE.md`
  - تم تخصيصه لإصدار v2
  - إضافة شرح هدف المشروع (لوحة تحكم + موقع لشركات المقاولات بدون حضور رقمي)
  - إضافة خطوات نشر تشمل Laravel + Node.js (npm build)
  - إضافة دليل تخصيص من لوحة التحكم + كيف ينعكس على الموقع

---

## قائمة مختصرة بأهم الملفات التي أُضيفت/تغيرت

### ملفات جديدة
- `app/Http/Controllers/Admin/EditorUploadController.php`
- `app/Http/Requests/Admin/EditorImageUploadRequest.php`
- `resources/views/admin/partials/summernote.blade.php`
- `RELEASE_TEMPLATE.md`
- (تم إنشاء عدة Requests في `app/Http/Requests/Admin/*.php`)

### ملفات تم تعديلها
- `routes/web.php` (إضافة route رفع صور المحرر داخل admin)
- `resources/views/admin/layouts/app.blade.php` (meta csrf)
- صفحات admin التي تستخدم `.js-editor` (استدعاء partial + context)
- Controllers:
  - `Admin/NewsController.php`
  - `Admin/PageController.php`
  - `Admin/ProjectController.php`
  - `Admin/ServiceController.php`
  - `Admin/TenderController.php`
  - `Admin/JobController.php`
  - `Admin/ClientController.php`
  - `Admin/SettingController.php`
- `app/Models/Setting.php` (كاش + تفريغ)
- `app/Http/Controllers/SiteController.php` (كاش الواجهة)
- `tests/Feature/ExampleTest.php` (RefreshDatabase)

---

## خطة اختبار يدوية سريعة للمراجعة (بدون تغيير واجهات)

### (أ) اختبار المحرر ورفع الصور
1) دخول `/admin` ثم فتح صفحة فيها محرر (مثال: الأخبار أو من نحن).
2) إدراج صورة داخل المحرر.
3) التأكد:
   - تظهر فوراً داخل المحرر (معاينة).
   - بعد ثوانٍ تستبدل بالرابط النهائي.
4) الضغط على “حفظ” بسرعة أثناء الرفع:
   - يجب أن ينتظر ثم يحفظ تلقائياً بعد اكتمال الرفع.
5) فتح الصفحة المقابلة في الموقع والتحقق من ظهور الصورة.

### (ب) اختبار مسار الوسائط
- افتح رابط صورة محفوظة مثل:
  - `/media/editor/news/....jpg`
وتأكد أنها تفتح 200 وتعرض الصورة.

### (ج) اختبار الأداء والكاش
1) فتح الصفحة الرئيسية أكثر من مرة ومراقبة زمن الاستجابة (يجب يتحسن).
2) تغيير قيمة من الإعدادات (مثل شعار/نص) ثم تحديث الصفحة:
   - خلال دقائق قليلة يجب أن تظهر التغييرات (أو بعد `php artisan optimize:clear` أثناء التطوير).

---

## ملاحظات للمطورين (مخاطر/اعتبارات)
- منع حفظ `blob:` داخل DB تم عبر intercept للـ submit على مستوى `form` داخل partial؛ هذا مقصود لتفادي حفظ محتوى غير مكتمل.
- الكاش المستخدم يعتمد على Driver إعدادات Laravel (file/database/redis).  
  إن كان الكاش “file” على shared hosting فهو مقبول، وإن توفر Redis سيكون أفضل.
- في بيئات الإنتاج، تأكد من:
  - `storage:link`
  - صلاحيات `storage/` و `bootstrap/cache`
  - تشغيل `npm run build` إن كان هناك Vite/Frontend build

