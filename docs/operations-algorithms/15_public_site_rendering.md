# خوارزمية عرض الموقع العام

## الهدف

شرح كيف يتحول محتوى لوحة التحكم إلى صفحات عامة يراها الزائر، مع إخفاء غير المنشور وجلب الإعدادات والقائمة تلقائيا.

## الملفات المرتبطة

- `app/Http/Controllers/SiteController.php`
- `app/Providers/AppServiceProvider.php`
- `app/Models/Setting.php`
- `routes/web.php`
- `resources/views/site/*`

## خوارزمية حقن الإعدادات والقائمة في كل صفحة

```mermaid
flowchart TD
    A["أي view يبدأ بـ site.*"] --> B["View::composer"]
    B --> C["جلب settings إذا كان الجدول موجودا"]
    C --> D["parseValue لكل setting"]
    D --> E["جلب الصفحات المنشورة"]
    E --> F["بناء defaultMenu"]
    F --> G{"clients_page_enabled؟"}
    G -- "نعم" --> H["إضافة رابط عملاؤنا"]
    G -- "لا" --> I["عدم إضافة الرابط"]
    H --> J["فحص site_menu المخصص"]
    I --> J
    J --> K["حقن siteSettings/sitePages/siteMenu"]
```

## خوارزمية الصفحة الرئيسية

```mermaid
flowchart TD
    A["GET /"] --> B["جلب الخدمات المنشورة مع كاش 5 دقائق"]
    B --> C["جلب المشاريع المنشورة مع كاش 5 دقائق"]
    C --> D["جلب آخر الأخبار المنشورة مع كاش دقيقتين"]
    D --> E["جلب العملاء المنشورين المرتبطين بمشاريع منشورة"]
    E --> F["جلب siteSettings"]
    F --> G["عرض site.index"]
```

## خوارزمية صفحات التفاصيل

```mermaid
flowchart TD
    A["GET /services/{slug} أو /projects/{slug} أو /news/{slug}"] --> B["البحث حسب slug"]
    B --> C{"هل السجل موجود؟"}
    C -- "لا" --> D["404"]
    C -- "نعم" --> E["جلب عناصر مرتبطة أو مشابهة"]
    E --> F["عرض صفحة التفاصيل"]
```

## خوارزمية البحث العام

```mermaid
flowchart TD
    A["GET /search?q=..."] --> B["validate q max 120"]
    B --> C["toLikePattern مع هروب % و _"]
    C --> D["البحث في الخدمات المنشورة"]
    D --> E["البحث في المشاريع المنشورة"]
    E --> F["البحث في الأخبار والوظائف والمناقصات والصفحات المنشورة"]
    F --> G["عرض نتائج موحدة"]
```

## خوارزمية الوسائط

```mermaid
flowchart TD
    A["طلب /media/{path}"] --> B["فحص وجود الملف في public disk"]
    B --> C{"هل الملف موجود؟"}
    C -- "لا" --> D["404"]
    C -- "نعم" --> E["Storage::response(path)"]
```

## نتيجة العملية

الموقع العام لا يقرأ كل شيء عشوائيا؛ بل يعتمد على قواعد نشر وكاش وإعدادات مركزية، لذلك يرى الزائر نسخة مرتبة ومفلترة من المحتوى الذي تديره الشركة.
