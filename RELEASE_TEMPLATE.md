# Release v2 — منصة رقمية لشركات المقاولات

> ملاحظة: هذا القالب مخصص لصفحة **Releases** في GitHub.  
> انسخه كما هو عند إنشاء Release جديد وعدّل الأقسام حسب التغييرات الفعلية.

## ملخص
- **هدف المشروع**: إنشاء تطبيق ويب متكامل (**لوحة تحكم + موقع**) مخصص لشركات المقاولات التي لا تمتلك حضوراً رقمياً، لعرض الخدمات والمشاريع والأخبار والمناقصات والوظائف وإدارة المحتوى بسهولة من مكان واحد.
- **القيمة**: تمكين الشركة من إطلاق موقع احترافي بسرعة، مع إدارة مركزية للمحتوى والهوية البصرية والبيانات التشغيلية دون الحاجة لفريق تقني دائم.

## أبرز التغييرات
### مميزات جديدة
- **لوحة تحكم (Admin Dashboard)** لإدارة المحتوى (خدمات/مشاريع/أخبار/صفحات/مناقصات/وظائف/عملاء) والتحكم بحالة النشر.
- **موقع عام (Public Site)** يعرض المحتوى المنشور مع صفحات أرشيف وتفاصيل.
- **إعدادات وهوية بصرية** (شعارات/ألوان/بيانات شركة/روابط تواصل) تنعكس مباشرة على الواجهة.
- **محرر نصوص مع رفع صور احترافي**: رفع الصورة عند الإدراج وتخزين رابطها داخل المحتوى بدل Base64 لتحسين الأداء وتقليل حجم قاعدة البيانات.

### تحسينات
- **تحسين الاستجابة والأداء** عبر كاش للأجزاء الأكثر طلباً في الواجهة (إعدادات الموقع، الصفحة الرئيسية، صفحات الأرشيف والتفاصيل).

### إصلاحات
- 

## تغييرات قد تؤثر على المستخدم (Breaking / Behavior Changes)
- **هل توجد تغييرات كاسرة؟**: لا / نعم
  - **التفاصيل**:
    - 
  - **طريقة الترقية**:
    - 

## ترقية وتشغيل (Deployment / Upgrade)
### متطلبات
- PHP: `>= 8.2`
- Laravel: `12.x`
- Node.js: `>= 18` (لبناء ملفات الواجهة/الأصول إن كان المشروع يستخدم Vite أو أدوات Frontend)

### خطوات الترقية المقترحة
1) أخذ نسخة احتياطية من قاعدة البيانات والملفات (خصوصاً `storage/`).
2) تحديث الكود على السيرفر.
3) تثبيت اعتماديات PHP:

```bash
composer install --no-dev --optimize-autoloader
```

4) تثبيت اعتماديات Node.js (إن كان مشروعك يبني CSS/JS عبر Node):

```bash
npm ci
```

5) بناء ملفات الواجهة (Production build):

```bash
npm run build
```

6) تنفيذ الترحيلات:

```bash
php artisan migrate --force
```

7) تجهيز التخزين العام (إن لم يكن موجوداً):

```bash
php artisan storage:link
```

8) تنظيف/بناء كاش Laravel:

```bash
php artisan optimize:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

## دليل التخصيص من لوحة التحكم (Customization Guide)
> الهدف: تمكين العميل من تعديل الموقع بالكامل بدون لمس الكود.

### الدخول إلى لوحة التحكم
- افتح: `/admin`
- سجّل الدخول بحساب الإدارة.

### أين تعدّل المحتوى؟
- **الخدمات**: إضافة/تعديل خدمة، صورة، وصف مختصر وتفصيلي، حالة النشر.
- **المشاريع**: إضافة/تعديل مشروع وربطه بخدمة، صورة، تفاصيل، حالة النشر.
- **الأخبار**: إضافة/تعديل خبر، محتوى عبر المحرر، صورة، حالة النشر وتاريخ النشر.
- **الصفحات**: إنشاء صفحات ثابتة (مثل سياسة الخصوصية/الشروط) وتحريرها عبر المحرر.
- **المناقصات**: إدارة مناقصات المنشأة، تفاصيل، حالة النشر، تاريخ الإغلاق.
- **الوظائف**: إدارة الوظائف الشاغرة، متطلبات/مهارات، حالة النشاط وتاريخ الإغلاق.
- **العملاء**: إضافة شعارات العملاء وربطهم بمشاريع فعلية، والتحكم بظهور صفحة "عملاؤنا".

### تخصيص الهوية البصرية (Branding)
- من: **إعدادات الموقع** → **الهوية البصرية**
  - **الشعار / الشعار الشفاف / favicon**
  - **ألوان الثيم** (أو اختيار Preset جاهز)
  - **بيانات الشركة** وروابط التواصل
  - **صورة الهيرو في الصفحة الرئيسية** ونصوصها

### ملاحظة مهمة للمحرر (Editor)
- الصور داخل المحرر تُرفع تلقائياً عند إدراجها، ويُحفظ **رابط الصورة فقط** داخل المحتوى (بدون Base64) لتحسين الأداء وتقليل حجم قاعدة البيانات.

## كيف ينعكس ذلك على الموقع؟ (How the website is rendered)
- أي عنصر تم حفظه وهو **منشور/مفعّل** يظهر تلقائياً في الواجهة العامة.
- صفحات الواجهة الرئيسية:
  - `/` الرئيسية: خدمات مختارة + مشاريع + آخر الأخبار + (عملاء إن كانت الصفحة مفعلة).
  - `/services` الخدمات + `/services/{slug}` تفاصيل الخدمة
  - `/projects` المشاريع + `/projects/{slug}` تفاصيل المشروع
  - `/news` الأخبار + `/news/{slug}` تفاصيل الخبر
  - `/tenders` المناقصات
  - `/careers` الوظائف
  - `/pages/{slug}` الصفحات الثابتة


## خطة اختبار (Test Plan)
- [ ] فتح الصفحة الرئيسية والتأكد من تحميل الأقسام الأساسية بدون أخطاء.
- [ ] تجربة إنشاء/تعديل محتوى من لوحة التحكم وحفظه بنجاح.
- [ ] التحقق من رفع الصور من المحرر وظهورها داخل المحتوى بعد الحفظ.
- [ ] اختبار صفحات الأرشفة (أخبار/مشاريع/خدمات/وظائف/مناقصات) والتنقل بين الصفحات.

## ملاحظات تقنية (للفريق)
- **الأداء**:
  - 
- **الكاش**:
  - 
- **قاعدة البيانات**:
  - 

## أمن وخصوصية
- **تغييرات أمنية**:
  - لا يوجد / يوجد
- **ملاحظات**:
  - 

## شكر / مساهمات
- 

